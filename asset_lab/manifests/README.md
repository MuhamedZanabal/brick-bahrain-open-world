# Asset Manifests

This root is reserved for source/generated metadata. Phase 3 will add the authoritative JSON schema and validated asset manifests. A manifest must not be accepted without that schema.
