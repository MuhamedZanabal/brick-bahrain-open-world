name: Bahrain Brick R1 Renderer Runtime Debugging — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'R1 diagnostic execution is bounded to adjudicative run 30179849775. Do not rerun automatically.'
