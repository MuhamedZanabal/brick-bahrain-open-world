name: Bahrain Brick R1 GL Budget Fix — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Workflow inactive while the GL Compatibility light-budget fix is committed.'
