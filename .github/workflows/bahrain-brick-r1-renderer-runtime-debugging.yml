name: Bahrain Brick R1 Targeted Android Verification — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Targeted R1 workflow is inactive while the Godot 4.7.1 fix is committed atomically.'
