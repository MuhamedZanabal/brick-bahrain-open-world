name: Cancel Duplicate G0 Evidence Runs — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    if: false
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Duplicate cancellation completed in run 30088001461. Automatic cancellation is archived.'
