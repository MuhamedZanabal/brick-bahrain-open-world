name: Bahrain Brick R1 Publish Physical APKs — Archived

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - name: Report archived publication
        run: |
          echo 'R1 physical diagnostic APKs were already published from retained run 30385101264.'
          echo 'Automatic republishing is archived to prevent diagnostic packages being mistaken for playable builds.'
          exit 64
